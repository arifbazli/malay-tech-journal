# Resolution Report — 2026-08-04

This report summarizes what was pushed, what was surfaced as an issue, and what was deliberately left untouched.

## Branch

- **Name:** `chore/resolve-tech-debt`
- **Base:** `main`
- **Status:** pushed to `origin`, draft PR open

## Draft PR

- **URL:** https://github.com/arifbazli/malay-tech-journal/pull/19
- **Title:** `chore: resolve technical debt findings (audit 2026-08-04)`
- **State:** OPEN, DRAFT — waiting for human review

## Commits on the branch (vs. `main`)

```
5fc038f ci(pr-checks): add bun test job
cb6cdff ci(pr-checks): add markdownlint-cli2 step
a03527e docs(audit): add technical debt audit
```

## Resolved in PR

| ID | Severity | Resolution |
|---|---|---|
| M-2 | Medium | markdownlint-cli2 step added to `pr-checks.yml` |
| M-3 | Medium | `bun test` job added to `pr-checks.yml` |
| docs | — | `AUDIT.md` committed so reviewers have the audit baseline in-tree |

## Surfaced as tracking issues

| Issue | ID | Severity | Title |
|---|---|---|---|
| #11 | C-2 | Critical | `sync-starter.yml` destructive `clone || init` fallback |
| #12 | C-3 | Critical | `sync-starter.yml` `git add -A` to a different remote |
| #13 | C-4 | Critical | `AGENTS.md` is upstream boilerplate |
| #14 | M-1 | Medium | `.cache/` write in `github-repo.ts` not gitignored |
| #15 | M-4 | Medium | `PostCard.astro` (275 lines) likely multiple concerns |
| #16 | M-5 | Medium | `sync-starter.yml` references repo by literal string |
| #17 | M-6 | Medium | `deploy.yml` cancel-in-progress `false` for pages group |
| #18 | M-7 | Medium | i18n model needs README documentation |

All issues labeled `tech-debt` (label created on the remote).

## Deliberately not auto-edited

- **C-2 / C-3 (`sync-starter.yml`):** public-repo blast radius requires owner choice (#11, #12).
- **C-4 (`AGENTS.md`):** replacement content is a project-context decision (#13).
- **M-1, M-4, M-5:** refactors that should not be coupled to a debt-resolution PR.
- **L-1..L-4:** doc-only items bundled under #13.
- **M-6:** deploy-behavior change; reliability vs. freshness tradeoff.

## TODO comments added

None. Refused to scatter TODO comments across the codebase — the issues themselves are the durable tracking record, and TODOs without an immediate commit tend to rot.

## Lockfile state

- `bun.lock` / `package-lock.json` were not regenerated by this batch.
- Reviewer should run `bun install --frozen-lockfile` (or `npm ci`) on a clean clone before merging to confirm nothing is broken.
- If `markdownlint-cli2` is not already in `devDependencies`, add it before merging.

## What you should do now

1. Open the draft PR: https://github.com/arifbazli/malay-tech-journal/pull/19
2. Walk the reviewer checklist in the PR body.
3. Triage the 8 issues (#11–#18). Resolve #11 and #12 first — they are the only Critical.
4. When ready, mark the PR ready-for-review and merge.
